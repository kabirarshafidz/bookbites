<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="styles.css" />
  </head>
  <body>
    <form action="/" method="get"><button type="submit">Back</button></form>
    <% if (locals.book) { %>
    <h1><%= heading %></h1>
    <form action="/edit/<%= book.id %>" method="post">
      <input
        name="title"
        type="text"
        placeholder="Title"
        required
        value="<%= book.title %>"
      />
      <input
        name="isbn"
        type="number"
        placeholder="ISBN"
        onkeypress="return /[0-9]/i.test(event.key)"
        minlength="10"
        maxlength="13"
        value="<%= book.isbn %>"
      />
      <input
        name="author"
        type="text"
        placeholder="Author"
        value="<%= book.author %>"
      />
      <input name="date" type="date" value="<%= book.date %>" required />
      <input
        name="rating"
        type="number"
        onkeypress="return /[0-9||.]/i.test(event.key)"
        min="0"
        max="10"
        step="0.1"
        value="<%= book.rating %>"
      />
      <input
        name="note"
        type="text"
        placeholder="Write your note!"
        value="<%= book.note %>"
      />
      <button type="submit"><%= submit%></button>
    </form>
    <% } else { %>
    <h1><%= heading %></h1>
    <form action="/new" method="post">
      <input name="title" type="text" placeholder="Title" required />
      <input
        name="isbn"
        type="number"
        placeholder="ISBN"
        onkeypress="return /[0-9]/i.test(event.key)"
        minlength="10"
        maxlength="13"
      />
      <input name="author" type="text" placeholder="Author" />
      <input name="date" type="date" required />
      <input
        name="rating"
        type="number"
        onkeypress="return /[0-9||.]/i.test(event.key)"
        min="0"
        max="10"
        step="0.1"
      />
      <input name="note" type="text" placeholder="Write your note!" />
      <button type="submit"><%= submit%></button>
    </form>
    <% } %>
  </body>
</html>
